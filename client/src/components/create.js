import { useState } from "react";
import axios from 'axios';

const Create = (props) => {
  const handleClick = async () => {

  }

  return (
    <button
      onClick={() => handleClick()}
    >
      Add
    </button>
  );
}

export default Create;